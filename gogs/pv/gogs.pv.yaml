apiVersion: v1
kind: PersistentVolume
metadata:
  name: gogs-data-pv
  labels:
    type: gogs-data-pv
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle
  nfs:
    path: /opt/k8s/gogs/data
    server: 10.45.10.148