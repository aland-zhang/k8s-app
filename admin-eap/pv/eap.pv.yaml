apiVersion: v1
kind: PersistentVolume
metadata:
  name: eap-data-pv
  labels:
    type: eap-data-pv
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle
  nfs:
    path: /opt/kubernetes/eap/data
    server: 10.0.0.191
