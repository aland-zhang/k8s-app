apiVersion: v1
kind: PersistentVolume
metadata:
  name: eap-storage-pv
  labels:
    type: eap-storage
spec:
  capacity:
    storage: 30Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle
  nfs:
    path: /opt/kubernetes/eap/mysql
    server: 10.0.0.191
