apiVersion: v1
kind: PersistentVolume
metadata:
  name: eap-redis-pv
  labels:
    type: eap-redis-pv
spec:
  capacity:
    storage: 30Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Recycle
  nfs:
    path: /opt/kubernetes/redis/
    server: 10.0.0.191